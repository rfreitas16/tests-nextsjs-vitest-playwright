// Para executar o comando:  npm run drizzle:generate -> drizzle-kit generate
// certinho no windows é prociso acessar o arquivo de configuracao .env - config.ts
// e enviar o valor com as barras para frente em formato de string,
// se usar join gera um erro. Apos executar o npm run drizzle:generate deve ser
// feita a migracao para ele executar o comando e criar a tabela do banco usar:
// drizzle:migrate:dev para desenvolvimento ou drizzle:migrate:prod para gerar
// outro db para producao deve retornar um array vazio se der certo.
